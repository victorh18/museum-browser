import { style } from "@angular/animations";

export const STYLES = {
    SCREENBASE: style({
        height: '92%',
        position: 'absolute',
        left: 0,
        width: '100%',
        opacity: 0,
        transform: 'scale(0.3) translateY(30%)',
    })
}